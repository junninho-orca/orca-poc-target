[pytest]
# Scoped explicitly. Without this, pytest walks the whole repo from the root and would
# collect from `node_modules/` (the Node service's dependencies) and `.python-packages/`
# (this service's, installed there so they survive into the offline test container).
testpaths = tests_py
norecursedirs = node_modules .python-packages .git
